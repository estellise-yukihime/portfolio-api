name: release.yml

on:
  release:
    types: [ released ]

jobs:
  fetch:
    runs-on: desperation
    outputs:
      rebuild: ${{ steps.inspect.outcome == 'failure' }}
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v6
      - name: login registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: inspect image
        id: inspect
        continue-on-error: true
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: |
          VERSION=${TAG#v}
          docker buildx imagetools inspect ghcr.io/${{ github.repository }}:$VERSION

          if [[ $? -ne 0 ]]; then
            echo "::error failed to download image"
            exit 1
          fi

  build:
    needs: fetch
    if: ${{ needs.fetch.outputs.rebuild == 'true' }}
    uses: ./.github/workflows/prerelease.yml
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    secrets: inherit

  deploy:
    needs: [ fetch, build ]
    if: ${{ always() && (needs.fetch.result == 'success' || (needs.build.result == 'skipped' || needs.build.result == 'success')) }}
    runs-on: desperation
    steps:
      - uses: actions/checkout@v6
      - name: compose up
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
        run: |
          VERSION=${TAG_NAME#v}
          VERSION=$VERSION docker compose up -d

